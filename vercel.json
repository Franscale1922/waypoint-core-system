{
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/guard-immutable-tables.mjs && prisma db push && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "feat/match-workspace-foundation": true
    }
  }
}
